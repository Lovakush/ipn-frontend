# OriginLeadCustomer.xml

**Path**: `config\api_platform\properties\OriginLeadCustomer.xml`

## Summary
# Summary

This XML configuration file defines metadata properties for the `OriginLeadCustomer` resource in an API Platform application, specifically declaring the `id` field as a read-only identifier. The file establishes the schema structure that governs how this entity's properties are exposed and validated through the API Platform framework.

